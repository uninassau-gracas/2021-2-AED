#include <stdio.h>
#include <stdlib.h>

int main()
{
    // Exemplo com tipo de dado caractere
    // Definir e inicializazr a variável
    char nome_aluno[60] = "Jack Daniel's";
    // Acessar a variável imprimindo seu valor em tela
    printf("Imprimindo o valor da variavel: %s", nome_aluno);

    return 0;
}
